---
layout: default
---

# 仕様からチェックリストを自動生成し監査するエージェント

<div class="mt-2">

```mermaid {scale: 0.8}
graph LR
    SPEC[仕様文書] --> |チェックリスト自動生成| REQ[要件1,2,...]
    REQ --> MAP[実装との対応]
    MAP --> |監査| REPORT[脆弱性報告書]
```

</div>

<div class="grid grid-cols-2 gap-6 mt-2">

<div class="p-3 bg-blue-50 border-l-4 border-blue-500">

**差分ではなく仕様を根拠に**

各実装を独立に仕様と照合

</div>

<div class="p-3 bg-green-50 border-l-4 border-green-500">

**1つの発見 → 多実装へ横展開**

同じチェックリストを全実装に適用

</div>

</div>

<!--
そこで我々は、仕様をもとにチェックリストを自動で生成し、ソースコードの監査を行うLLMエージェントを開発しました。

このエージェントは、仕様文書からMUSTやSHOULDといった要件を抽出してチェックリストを自動生成し、それを各実装に独立に適用します。

これにより、実装間の差分ではなく、仕様そのものを正しさの根拠とできます。

さらに重要なのは、ある実装で発見したバグパターンを、同じチェックリストを用いて他の実装にも横展開できる点です。
-->
